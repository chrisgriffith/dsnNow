import { SpacecraftPipe } from './spacecraft.pipe';

describe('SpacecraftPipe', () => {
  it('create an instance', () => {
    const pipe = new SpacecraftPipe();
    expect(pipe).toBeTruthy();
  });
});
